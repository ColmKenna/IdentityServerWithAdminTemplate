@page "{id}"
@model BasicsModel
@{
    ViewData["Title"] = "Edit Client Basics";
    ViewData["Breadcrumbs"] = new List<(string Text, string? Url)>
    {
        ("Admin", "/Admin/Clients/Index"),
        ("Clients", "/Admin/Clients/Index"),
        (Model.Input.ClientName ?? Model.Id, $"/Admin/Clients/Details/{Model.Id}"),
        ("Basics", null)
    };
}

<div class="page-header">
    <div>
        <h1 class="page-title">Edit Client Basics</h1>
        <p class="page-sub">Configure the basic identity and description details for this client application.</p>
    </div>
</div>

<partial name="_ClientEditorTabs" model="new ClientEditorTabsModel(Model.Id, ClientEditorTab.Basics)"/>

<form method="post" id="edit-client-basics-form">
    <div asp-validation-summary="All" class="validation-summary alert-panel alert-danger"
         style="display: @(ViewData.ModelState.IsValid ? "none" : "block"); margin-bottom: 20px;"></div>

    <div class="panel" style="margin-bottom: 24px; padding: 28px;">
        <h2 class="panel-title" style="font-size: 1.25rem; margin-bottom: 20px; font-weight: 700;">Client Basics</h2>

        <div class="form-field">
            <label class="form-label">Client ID</label>
            <input type="text" class="form-control" value="@Model.ClientIdDisplay" disabled/>
            <small class="form-text">The unique identifier for this client. This cannot be changed.</small>
        </div>

        <div class="form-field">
            <label asp-for="Input.ClientName" class="form-label">Client Display Name <span
                    class="required">*</span></label>
            <input asp-for="Input.ClientName" class="form-control" placeholder="e.g. Customer Portal Web" required/>
            <span asp-validation-for="Input.ClientName" class="text-danger field-validation-error"></span>
        </div>

        <div class="form-field">
            <label asp-for="Input.Description" class="form-label">Description</label>
            <textarea asp-for="Input.Description" class="form-control" rows="4"
                      placeholder="Optional notes about this client application"></textarea>
            <span asp-validation-for="Input.Description" class="text-danger field-validation-error"></span>
        </div>
    </div>

    <div class="form-actions" style="display: flex; gap: 12px; margin-top: 20px;">
        <button type="submit" class="btn-primary">Save Changes</button>
        <a asp-page="./Details" asp-route-id="@Model.Id" class="btn-secondary">Cancel</a>
    </div>
</form>

<style>
    .form-field {
        display: flex;
        flex-direction: column;
        gap: 6px;
        margin-bottom: 20px;
    }

    .form-control {
        width: 100%;
        max-width: 550px;
        border: 1px solid var(--border, #e6e8eb);
        border-radius: 8px;
        padding: 10px 14px;
        font-size: 0.95rem;
        background-color: #fff;
        color: var(--ink, #17191c);
        transition: border-color 0.15s ease-in-out, box-shadow 0.15s ease-in-out;
    }

    .form-control:focus-visible {
        outline: 2px solid var(--accent, #2f6bff);
        outline-offset: 1px;
    }

    .form-control:disabled {
        background-color: #f1f3f5;
        border-color: #e6e8eb;
        color: var(--text-dim, #8b9096);
        cursor: not-allowed;
    }

    .form-label {
        font-weight: 600;
        font-size: 0.9rem;
        color: var(--ink, #17191c);
    }

    .form-text {
        font-size: 0.825rem;
        color: var(--text-dim, #8b9096);
        margin-top: 2px;
    }

    .required {
        color: #c8433b;
    }

    .field-validation-error {
        font-size: 0.85rem;
        color: #c8433b;
        margin-top: 2px;
    }
</style>
